"""Benchmark engine and history for Ollama models.

- ``experiment`` — running prompts against models and comparing the timings
  and noise of the results.
- ``history`` — persisted comparison runs under the toolkit's data root.
"""
